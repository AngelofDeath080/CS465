# Architecture
The types of frontend developement that were utilized for this project were HTML,CSS,SPA,JavaScript and of course SPA. The reason that these were utilized were due to them being easier to implement and be flexable with each other allowing for a more seamless
process. As well as being able to adjust code much simpler than needing to write multiple lines of code for the same process. On the backend NoSQL and MongoDB were utilized to handle the database and implimantion of the database. As they have more adjustablity and testing
them was much soomther than hard coding.

# Functionality
JSON and JavaScript are different from each other due to how they are utilized as with JSON files they handle more of the structure and data-interchange between files. While with JavaScript handles the dynamic and interactive side of things, allowing
for webpages to be used. One area that was refactored code to improve the functionality would be on the app_admin as it needed to be SPA and able to adjust to screen size without multiple functions handling it. Once it was refactored into automatically adjsuting it allowed the front and backend to function soothmly.

# Testing
My understanding of testing API among methods, endpoints and security, are bit fresh still but in the general understanding much simpler than first thought. Due to testing methods they are more like a system function that doesnt require a lot of user interaction in order
to test if it's working. While with endpoints it's ensuring that the API is being linked to the specific url that is requested. And then finally the security side of things was a bit more complex as it's the first time implemnting a full on security system and testing was a bit more difficult than first thought.
But it allowed for seeing the internal workings of the security system and ensuring that it was functioning as intended.

# Reflection
This course has shown me that theres still much to learn in order to grow in being a fullstack developer and will point me towards which areas will need more practice to make it more practical for the profesional relm.

